#include <stdio.h>
#include <string.h>
#include <time.h>
#include <stdlib.h>

/* checking if input is negative or positive*/

/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
    
    #include <stdio.h>

	int main()
	{
int n = 0;

		        while (n <= 100) {
				        if (n > 0) {
						            printf("%d, the number is positive\n", n);
							            } else if (n < 0) {
									                printf("%d, the number is negative\n", n);
											        } else {
													            printf("%d, the number is zero\n", n);
														            }

					        n--;
						    }

			    return (0);
}

